[project]
name = "elt-ecommerce"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dbt-bigquery>=1.11.1",
    "dlt[bigquery]>=1.24.0",
]

[dependency-groups]
dev = [
    "kaggle>=2.0.0",
]
